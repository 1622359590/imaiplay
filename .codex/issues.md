# 问题与待决定事项

## 已决定

- 数据库选型：PostgreSQL（已决定）
- ORM 选型：GORM（已决定）
- Tenant code 创建后不可修改（已决定并已修复）。

## 待决定

1. 是否需要 Redis？第一阶段是否用本地缓存即可？
2. 前端是否独立仓库？是否后续再拆分？
3. superadmin 初始化脚本或专用引导机制的具体形式。
4. superadmin 后续采用独立登录入口还是复用认证接口。

## 已知约束

- 项目必须从零开发，不能复用 PlayEdu 代码。
- 多租户第一阶段采用共享数据库 + 租户字段。
- Tenant `Status` 保持 `int`；受 GORM `default:1` 零值语义影响，创建时显式 `0` 会使用默认值 `1`。
- 第一阶段 RBAC 仅使用固定角色判断，不实现复杂 ACL。
- 用户 Repository 按 JWT 中的 `tenant_id` 强制过滤按 ID 的读写操作。
- 租户仍有用户时删除会由外键拒绝；当前返回 500，后续可细化为 409。
- 重复键已映射为 409，后续可补充直接注入并发冲突的 Service 测试。
- 学员课程暂按当前租户及发布状态过滤，课程指派留待后续任务。
- 三个前端生产包超过 Vite 500 kB 提示，后续可按路由拆包。

## 已修复

- 配置加载路径依赖工作目录的问题已修复。
- 开发机已安装 Go 1.26.5，无需临时工具链。

## 暂无阻塞问题
