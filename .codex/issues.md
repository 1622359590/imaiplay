# 问题与待决定事项

## 已决定

- 数据库选型：PostgreSQL（已决定）
- ORM 选型：GORM（已决定）

## 待决定

1. 是否需要 Redis？第一阶段是否用本地缓存即可？
2. 前端是否独立仓库？是否后续再拆分？

## 已知约束

- 项目必须从零开发，不能复用 PlayEdu 代码。
- 多租户第一阶段采用共享数据库 + 租户字段。
- Tenant `Status` 保持 `int`；受 GORM `default:1` 零值语义影响，创建时显式 `0` 会使用默认值 `1`。

## 已修复

- 配置加载路径依赖工作目录的问题已修复。
- 开发机已安装 Go 1.26.5，无需临时工具链。

## 暂无阻塞问题
